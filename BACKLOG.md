# NBA Trade Analyzer — Backlog

This file tracks future features and improvements for the NBA Trade Analyzer project.

## How to Add Items

Copy the row template below and fill in all fields. Insert new items under the table, sorted by importance (Critical → High → Medium → Low). Use YYYY-MM-DD for the date. Status must be one of: `Pending`, `In Progress`, `Done`.

```
| **Name** | Description of the feature. | Server, Web App, AI Wrapper | YYYY-MM-DD | High | Pending |
```

---

## Backlog Items

| Name | Description | System Interactions | Date Added | Importance | Status |
|------|-------------|---------------------|------------|------------|--------|
| Roster Mismatch with HoopsHype | The roster returned for the New York Knicks (and possibly other teams) doesn't match what HoopsHype shows. Likely a name-matching issue between balldontlie and HoopsHype, or a scraping/pagination problem. Investigate and fix the cross-reference logic in `nba.js`. | Server | 2026-03-22 | High | Done |
| AI Uses Outdated Team Context | The AI model references outdated information (e.g. wrong coach for the Knicks). The prompt should include current coaching staff and any other relevant context that the model's training data may not cover, so the analysis reflects the real current state of each team. | Server | 2026-03-22 | High | Done |
| Fix Salary Calculation | Salary now shows current-season salary AND total remaining contract value (sum of all future seasons from HoopsHype). Previously only showed a single season's salary. Updated server scraping, prompt, and UI. | Server, Web App | 2026-03-22 | High | Done |
| Per-Team Trade Grade | Claude now returns a letter grade (A+ through F) for each team. Grades are parsed from the markdown and displayed as color-coded cards (green for good, yellow for mid, red for bad) above the analysis. | Server, Web App | 2026-03-22 | High | Done |
| Remove Claude Mentions from UI | Replaced all user-facing "Claude" references with neutral "AI" phrasing — tagline, footer, and analysis header. Backend/code variable names unchanged. | Web App | 2026-03-22 | High | Done |
| Fix Live Status Text | Replaced the "Live" streaming badge with "Analyzing trade..." and the "Complete" badge with "Analysis complete" for clearer user feedback during analysis. | Web App | 2026-03-22 | High | Done |
| API Rate Limits & Data Source Alternatives | Current setup relies on balldontlie (free tier: 5 req/min, no stats/season_averages endpoints) and HoopsHype scraping (fragile, no API contract). Research alternative NBA data APIs — what free options exist (e.g. nba_api, SportsData.io free tier, ESPN hidden APIs), what are the cheapest paid options (e.g. balldontlie paid tiers, Sportradar, RapidAPI NBA endpoints), and which gives the best coverage (rosters, salaries, stats, contracts) for the lowest cost. Recommend a migration path. | Server | 2026-03-23 | High | Pending |
| **[BACKEND] Cap Thresholds Data Store** | Add NBA CBA financial thresholds for the current season (salary cap line, luxury tax line, 1st apron, 2nd apron) as a config or DB collection. These values are the foundation for all cap-status and trade-legality calculations. For 2025-26: cap ≈ $141.1M, tax ≈ $171.3M, 1st apron ≈ $178.1M, 2nd apron ≈ $188.9M. Must be easy to update per season. | Server | 2026-03-26 | High | Pending |
| **[BACKEND] Per-Team Cap Position Calculation** | Compute each team's total active salary (sum of all non-two-way player salaries) and classify their cap status: under cap, over cap (taxpayer), 1st apron (hard-capped), or 2nd apron (hard-capped). Expose this as part of the `GET /api/teams/:id` or a new `GET /api/teams/:id/cap` endpoint. Used by the cap status banner in the UI and as input to trade legality. | Server | 2026-03-26 | High | Pending |
| **[BACKEND] Trade Legality Engine — CBA Salary Matching** | Implement CBA salary matching rules per trade side. For teams over the cap: incoming salary must be ≤ outgoing × 1.25 + $250K (for outgoing > $6.5M) or ≤ outgoing + 150% (for outgoing ≤ $6.5M). For teams under the cap: can absorb up to their remaining cap space + $100K. Return per-team result: `{ valid: bool, outgoingCap, allowableIncoming, incomingCap, capDifference, formula, apronStatusAfterTrade }`. Must be computed server-side before AI analysis. | Server | 2026-03-26 | High | Pending |
| **[BACKEND] Contract Details Enrichment** | Extend the player schema and HoopsHype scraper to capture: `yearsRemaining` (int), `contractType` (rookie/veteran-min/max/bird/two-way/10-day), `isExpiring` (bool, true if yearsRemaining === 1 or 0 after this season). These fields power the "Expiring Contract" label, contract type indicator, and non-tradable logic. | Server | 2026-03-26 | High | Pending |
| **[BACKEND] Trade Candidate & Non-Tradable Flags** | Add `isTradeable` (bool) and `tradeStatus` (enum: `tradeable`, `trade-candidate`, `not-tradable`) to the player schema. Flag non-tradable for: players signed within the last 3 months (recently signed restriction), 10-day contracts, and two-way contracts. Flag trade candidates based on HoopsHype or scrape the Spotrac trade machine's own designation. Include reason string for non-tradable (e.g., `"Recently signed — ineligible until Jan 15"`). | Server | 2026-03-26 | High | Pending |
| **[FRONTEND] Financial Breakdown in Trade Results** | Add a per-team "Financial Details" section to the results page (between grade cards and AI analysis). Show: outgoing cap, allowable incoming cap, incoming cap, cap difference, salary matching formula used (e.g., `$54.1M × 1.25 + $250K`), apron status after trade (with color-coded label), and tax/1st apron/2nd apron space remaining with deltas. Data comes from the trade legality engine response. Design reference: Spotrac image 3 + FanSpo image 4. | Web App | 2026-03-26 | High | Pending |
| **[FRONTEND] Trade Legality Banner** | Show a banner at the top of the results section indicating trade validity. Green "Trade valid" if both teams pass salary matching. Red "Trade invalid — [Team X] exceeds allowable incoming by $Y" if any side fails. Also surface soft warnings (e.g., "MIL must waive 1 player before processing") as a blue info banner below. Design reference: Spotrac image 3. | Web App | 2026-03-26 | High | Pending |
| **[FRONTEND] Cap Status Banner in Player Selection** | Show each team's current cap status as a contextual line directly below the team name tabs in the player selection panel. Example: "This team is currently a 1st apron (hard-capped) team." Use color coding: grey = under cap, yellow = taxpayer, orange = 1st apron, red = 2nd apron. Design reference: Spotrac image 2. | Web App | 2026-03-26 | High | Pending |
| **[FRONTEND] Trade Candidate & Non-Tradable Badges on Player Rows** | Show a blue "Trade Candidate" badge and a grey "Not Tradable" badge on player rows in the player selection panel. Non-tradable players should have their "Add to trade" button disabled with a tooltip explaining the restriction (e.g., "Recently signed — ineligible until Jan 15"). Design reference: Spotrac image 2. | Web App | 2026-03-26 | High | Pending |
| **[FRONTEND] Contract Details in Player Rows** | Show contract details below the salary figure on each player row: years remaining (e.g., "+ 2 yr, $121M"), "Expiring Contract" label for last-year deals, and contract type badge (Rookie, Vet Min, Two-Way, 10-Day) where applicable. Data populated from the enriched player schema. Design reference: Spotrac image 2. | Web App | 2026-03-26 | High | Pending |
| **[BACKEND] Roster Count & Waiver Compliance Check** | After computing each trade side, check if either team's resulting roster would exceed 15 standard contracts. If so, include `rosterCompliance: { mustWaive: N, message: "Team X must waive N player(s) before processing the trade" }` in the trade legality response. Two-way and 10-day contracts count separately and should not trigger this. | Server | 2026-03-26 | Medium | Pending |
| **[BACKEND] Draft Picks Data** | Add a `draftPicks` array to the team schema: `{ year, round, originalTeam, notes }`. Populate from HoopsHype or an alternative source. Expose via `GET /api/teams/:id/picks`. Picks should be includable in the trade payload as `{ type: "pick", year, round, originalTeam }` alongside players. | Server | 2026-03-26 | Medium | Pending |
| **[FRONTEND] Draft Picks Tab in Player Selection** | Add a "Draft Picks" tab in the player selection panel alongside "Active Roster". List the team's owned picks (year, round, note on original team if not own). Allow picks to be added to the trade the same way players are — with an add/remove toggle. Update TradeSummary to show picks in the sends/receives list. | Web App | 2026-03-26 | Medium | Pending |
| **[FRONTEND] Team Selection Season Year Selector** | Add a season selector dropdown at the top of the team selection step (default: current season 2025-26). When changed, re-fetch salary and cap threshold data for that season. UI should match the clean dropdown style from Spotrac image 1. | Web App | 2026-03-26 | Medium | Pending |
| **[FRONTEND] Player Physical Attributes in Rows** | Add age, height, and weight as small subtext under the position label in each player row (e.g., "PG, 35 yo, 6'4'', 205 lbs"). Source from balldontlie player data. Subtle, secondary text — should not compete visually with salary. Design reference: Spotrac image 2. | Web App | 2026-03-26 | Medium | Pending |
| Automated Salary Sync | Replace HoopsHype scraping with a paid NBA stats API (e.g. Sportradar) for reliable, structured salary data. | Server | 2026-03-22 | Medium | Pending |
| Real Season Stats | Upgrade balldontlie plan or switch to a different stats provider to surface PPG/RPG/APG/MPG in the prompt and UI. | Server | 2026-03-22 | Medium | Pending |
| Three-Way Trade Support | Extend the trade model to support a third team. Requires UI changes (third TeamPanel), backend trade payload updates, and prompt restructuring. | Server, Web App | 2026-03-22 | Medium | Pending |
| Trade History Persistence | Save past trade analyses to a local database (SQLite or Postgres) so users can revisit previous trades. | Server | 2026-03-22 | Medium | Pending |
| Shareable Trade URLs | Encode the full trade state (teams, players) into URL query params so trades can be shared via link. | Web App | 2026-03-22 | Medium | Pending |
| Multiple AI Personas | Let users choose an analysis persona — e.g. Contender GM, Rebuilding GM, Fantasy Manager — each with a different prompt framing. | Server, Web App | 2026-03-22 | Medium | Pending |
| Docker Compose Setup | Add a `docker-compose.yml` to orchestrate all three services (client, app server, claude-wrapper) as a single stack for easy local setup. | Server, Web App, AI Wrapper | 2026-03-22 | Medium | Pending |
| Player Search / Autocomplete | Replace the full roster scroll with a search/autocomplete input to make player selection faster, especially for large rosters. | Web App | 2026-03-22 | Medium | Pending |
